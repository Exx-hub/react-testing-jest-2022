// import { render, screen } from "@testing-library/react";
// import App from "./App";

// test("renders learn react link", () => {
//   render(<App />); // render what component to test in the virtual dom.
//   const linkElement = screen.getByText(/learn react/i); // finding element with methods => interact with element with events
//   expect(linkElement).toBeInTheDocument(); // assert results are as expected
// });
